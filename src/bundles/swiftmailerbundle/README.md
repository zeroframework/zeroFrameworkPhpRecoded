Configuration
=============

Gmail example
-------------

        $serviceContainer["mailer.options"] = array(
            "server" => "localhost",
            "port" => 456,
            "security" => "ssl",
            "username" => "username",
            "password" => "password",
        );
